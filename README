# Chunk Tests

## Build

To build with cmake, please make sure the following libraries are installed:
 - GLFW
 - GLEW 
 - GLM

To build Cmake config, run `config.sh`

To build code, run `build.sh`

To run, run `run.sh`